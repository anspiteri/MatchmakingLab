[project]
name = "matchmaking_benchmarker"
version = "0.1.0"
description = "A framework for comparing competitive matchmaking algorithms."
authors = [{ name = "An Spiteri" }]
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
	"fastapi[standard]",
	"uvicorn[standard]"
]

[project.optional-dependencies]
dev = [
    "pytest"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
